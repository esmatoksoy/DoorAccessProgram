<Window x:Class="WpfApp1.MainWindow"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       Title="Door Access Control" Height="450" Width="800"
       MinWidth="800"
       MinHeight="450"
       Background="#E3F2FD">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="LightBlue" />
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#6EC6FF"/>
            <Setter Property="BorderThickness" Value="4"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="5,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                               BorderBrush="{TemplateBinding BorderBrush}"
                               BorderThickness="{TemplateBinding BorderThickness}"
                               CornerRadius="5"
                               SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                             VerticalAlignment="Center"
                                             RecognizesAccessKey="True"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="10" Background="#E3F2FD">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="129*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="73*"/>
            <RowDefinition Height="67*"/>
            <RowDefinition Height="274*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" HorizontalAlignment="Left" Margin="0,-26,0,26" Width="150" Grid.RowSpan="3" Grid.ColumnSpan="2">
            <TextBlock Text="Person ID:" Margin="0,20,0,0" FontSize="18" FontWeight="SemiBold" FontFamily="Source Sans Pro"/>
            <TextBox x:Name="txtPersonID" Margin="0,0,0,5" TextChanged="txtPersonID_TextChanged" PreviewTextInput="txtPersonID_PreviewTextInput"
   DataObject.Pasting="txtPersonID_Pasting"/>
            <TextBlock Text="Person Name:" FontSize="18" FontWeight="SemiBold"/>
            <TextBox x:Name="txtPersonName" Margin="0,0,0,5" PreviewTextInput="txtPersonName_PreviewTextInput"
             DataObject.Pasting="txtPersonName_Pasting"/>
            <TextBlock Text="Access Time:" FontSize="18" FontWeight="SemiBold" FontFamily="Source Sans Pro"/>
            <DatePicker x:Name="dpAccessTime" Margin="0,10,0,5"/>
            <TextBlock Text="Time (hh:mm):" FontSize="18" FontWeight="SemiBold"/>
            <TextBox x:Name="txtAccessOnlyTime" Margin="0,0,30,5" Width="120" MaxLength="5
                     " PreviewTextInput="txtAccessOnlyTime_PreviewTextInput"
             DataObject.Pasting="txtAccessOnlyTime_Pasting"/>
            <StackPanel Orientation="Vertical" Margin="0,10,0,0" VerticalAlignment="Top" Width="100">
                <Button Content="ADD" Margin="0,0,0,5" Click="Button_Click_Add"/>
                <Button Content="UPDATE" Margin="0,0,0,5" Click="Button_Click_Update"/>
                <Button Content="DELETE" Margin="0,0,0,5" Click="Button_Click_Delete"/>
                <Button Content="LIST ALL" Margin="0,0,0,5" Click="Button_Click_List"/>
                <Button Content="SEARCH" Click="Button_Click_Search"/>
                <Button Content="GRAPHICS" Width="90" Margin="5" Click="Button_Click_Graphics"/>

            </StackPanel>
        </StackPanel>
        <DataGrid x:Name="dgRecords" Margin="154,0,0,0" AutoGenerateColumns="True"
                 IsReadOnly="True" SelectionMode="Single" SelectionChanged="dgRecords_SelectionChanged" Grid.RowSpan="3" Grid.Column="1"/>
    </Grid>
</Window>
